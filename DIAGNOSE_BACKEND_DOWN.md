# 🔍 Диагностика: Backend не отвечает на маршруты

## 🚨 Текущая проблема

**Симптомы:**
- ✅ Health check работает: `GET /api/health` → `{"status":"ok"}`
- ❌ Все остальные маршруты: `{"error":"Route not found"}`
  - `/api/admin/seed` → Route not found
  - `/api/auth/login` → Route not found
  - Frontend → Route not found

## 🎯 Причина

Скорее всего **Backend падает сразу после запуска** из-за ошибки в коде или зависимостях.

**Почему работает только `/api/health`?**
- Health endpoint объявлен в `routes/index.ts` напрямую
- Остальные маршруты (`/admin`, `/auth`) подключаются через `adminRoutes`, `authRoutes`
- Если есть ошибка в этих файлах → приложение падает → маршруты не регистрируются

## 🔍 Как проверить логи на Render

### Вариант 1: Dashboard → Logs (Рекомендуется)

1. **Откройте Render Dashboard:**
   ```
   https://dashboard.render.com
   ```

2. **Найдите сервис:**
   - `rentcontrol-backend` (Web Service)

3. **Откройте вкладку "Logs":**
   - Ищите красные строки с ошибками
   - Обратите внимание на строки после `Server starting on port 3001`

4. **Что искать в логах:**
   ```
   ❌ ERROR: Cannot find module 'bcryptjs'
   ❌ ERROR: Cannot find module 'winston'
   ❌ SyntaxError: Unexpected token
   ❌ TypeError: Cannot read property '...' of undefined
   ❌ Error: Application exited early
   ```

### Вариант 2: Shell (если доступен)

```bash
# 1. Откройте Shell на Render
# 2. Проверьте логи приложения
cd /app
pm2 logs

# Или просто проверьте статус
pm2 status

# Или запустите вручную чтобы увидеть ошибку
npm start
```

## 🛠️ Возможные причины и решения

### 1. **Зависимости не установлены**

**Симптомы в логах:**
```
Error: Cannot find module 'bcryptjs'
Error: Cannot find module 'winston'
```

**Решение:**
```bash
# В Render Shell
cd /app
npm install
npm start
```

**Или обновите Build Command в Render:**
```bash
npm install && npx prisma generate && npm run build
```

### 2. **TypeScript не скомпилирован**

**Симптомы в логах:**
```
Error: Cannot find module './dist/index.js'
SyntaxError: Unexpected token 'export'
```

**Решение:**
- Убедитесь что Build Command включает `npm run build`
- Проверьте что `dist/` директория создана

### 3. **Ошибка в adminRoutes.ts**

**Симптомы:**
```
TypeError: Cannot read property 'use' of undefined
SyntaxError in adminRoutes.ts
```

**Решение:**
- Проверьте что последний коммит (`18869cb`) задеплоился
- Проверьте что нет syntax errors в `src/routes/adminRoutes.ts`

### 4. **База данных недоступна**

**Симптомы в логах:**
```
Error: P1001: Can't reach database server
Error: Connection timeout
```

**Решение:**
1. Проверьте `DATABASE_URL` в Environment Variables
2. Убедитесь что используете **Internal Database URL** (не External!)
3. Формат: `postgresql://user:password@hostname-internal/database`

### 5. **Prisma не сгенерирован**

**Симптомы:**
```
Error: @prisma/client did not initialize yet
Error: Cannot find module '@prisma/client'
```

**Решение:**
```bash
# В Render Shell
cd /app
npx prisma generate
npm start
```

**Или обновите Start Command:**
```bash
npx prisma generate && npm start
```

## 📋 Пошаговая диагностика

### Шаг 1: Проверьте логи на Render

1. Откройте https://dashboard.render.com
2. `rentcontrol-backend` → **Logs**
3. Скопируйте все ошибки (красные строки)
4. **Отправьте мне логи** - я помогу разобраться!

### Шаг 2: Проверьте Environment Variables

1. `rentcontrol-backend` → **Environment**
2. Проверьте что установлены:
   ```
   NODE_ENV=production
   PORT=3001
   DATABASE_URL=postgresql://... (Internal URL!)
   JWT_SECRET=your-secret-key
   JWT_EXPIRES_IN=7d
   CORS_ORIGIN=*
   ```

### Шаг 3: Проверьте Deploy Status

1. `rentcontrol-backend` → **Events**
2. Убедитесь что последний деплой **"Live"** (не "Failed")
3. Проверьте что деплой соответствует коммиту `18869cb`

### Шаг 4: Попробуйте Manual Deploy

1. `rentcontrol-backend` → **Manual Deploy**
2. Выберите **"Clear build cache & deploy"**
3. Нажмите **"Deploy"**
4. Дождитесь завершения (~5-7 минут)
5. Проверьте логи во время деплоя

## 🎯 Что делать прямо сейчас

### Вариант A: Если есть доступ к Shell

```bash
# 1. Откройте Shell на Render
# 2. Проверьте что приложение запущено
cd /app
npm start

# Смотрите на вывод:
# ✅ "Server starting on port 3001" - хорошо
# ❌ Любая ошибка - скопируйте и отправьте мне!
```

### Вариант B: Если Shell недоступен (Free tier)

**Отправьте мне логи из Dashboard:**

1. https://dashboard.render.com
2. `rentcontrol-backend` → **Logs**
3. Скопируйте **ВСЕ логи** (особенно красные строки)
4. Отправьте мне - я найду проблему!

## 🔧 Временное решение (если срочно)

Измените **Start Command** на упрощенный вариант:

```bash
npm start
```

Удалите seed из Start Command - запустим его позже через API.

**Как изменить:**
1. Render Dashboard → `rentcontrol-backend`
2. **Settings** → **Start Command**
3. Измените на: `npm start`
4. **Save Changes**
5. Дождитесь автоматического redeploy

## 📞 Что нужно от вас

**Отправьте мне:**
1. ✅ Логи из Render Dashboard (полностью, особенно ошибки)
2. ✅ Screenshot Logs если удобно
3. ✅ Текущий Start Command (Settings → Start Command)
4. ✅ Environment Variables (без паролей, но DATABASE_URL формат покажите)

**С этой информацией я быстро найду и исправлю проблему!** 🚀

---

## 📚 Связанные документы

- [QUICK_DEPLOY.md](./QUICK_DEPLOY.md) - Полная инструкция деплоя
- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - Решение проблем
- [TEST_BACKEND.md](./TEST_BACKEND.md) - Тестирование backend
- [SEED_VIA_API.md](./SEED_VIA_API.md) - Инициализация данных через API
