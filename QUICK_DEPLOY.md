# 🚀 Быстрый деплой на Render.com (5 минут)

Самый простой способ опубликовать RentControl Pro **БЕСПЛАТНО**.

## Шаг 1: Подготовка (30 секунд)

Убедитесь, что все изменения на GitHub:
```bash
cd /home/user/webapp
git status  # Проверьте, что нет незакоммиченных изменений
```

## Шаг 2: Регистрация на Render (1 минута)

1. Откройте [render.com](https://render.com)
2. Нажмите **"Get Started"**
3. Выберите **"Sign in with GitHub"**
4. Авторизуйте Render

## Шаг 3: Создание базы данных (1 минута)

1. На дашборде нажмите **"New +"**
2. Выберите **"PostgreSQL"**
3. Заполните:
   - **Name:** `rentcontrol-db`
   - **Database:** `rentcontrol_db`
   - **User:** `rentcontrol`
   - **Region:** Frankfurt (или ближайший)
   - **PostgreSQL Version:** 14
   - **Datadog API Key:** оставьте пустым
   - **Instance Type:** Free
4. Нажмите **"Create Database"**
5. ⏳ Подождите 1-2 минуты пока создастся
6. **ВАЖНО:** Скопируйте **Internal Database URL** (будет нужен в следующем шаге)

## Шаг 4: Деплой Backend (2 минуты)

### Вариант А: Docker (Alpine) - Рекомендуется ✅

1. Нажмите **"New +"** → **"Web Service"**
2. Выберите **"Build and deploy from a Git repository"**
3. Найдите и выберите ваш репозиторий **RC2**
4. Заполните:
   - **Name:** `rentcontrol-backend`
   - **Region:** Frankfurt
   - **Branch:** `main`
   - **Root Directory:** `server`
   - **Runtime:** Docker
   - **Dockerfile Path:** `Dockerfile` (по умолчанию)
   - **Instance Type:** Free

5. Нажмите **"Advanced"** и добавьте Environment Variables:

   | Key | Value |
   |-----|-------|
   | `NODE_ENV` | `production` |
   | `PORT` | `3001` |
   | `DATABASE_URL` | *вставьте Internal Database URL из шага 3* |
   | `JWT_SECRET` | *сгенерируйте случайную строку, например: `my-super-secret-jwt-key-2026`* |
   | `JWT_EXPIRES_IN` | `7d` |
   | `CORS_ORIGIN` | `*` (потом обновим) |

6. Нажмите **"Create Web Service"**
7. ⏳ Подождите 5-7 минут пока деплоится
8. **ВАЖНО:** Скопируйте URL backend (например: `https://rentcontrol-backend.onrender.com`)

### Вариант Б: Native Node.js (если Docker не работает)

Если у вас проблемы с Docker, используйте Native Environment:

4. Заполните:
   - **Name:** `rentcontrol-backend`
   - **Region:** Frankfurt
   - **Branch:** `main`
   - **Root Directory:** `server`
   - **Runtime:** Node
   - **Build Command:**
     ```
     npm install && npx prisma generate && npm run build
     ```
   - **Start Command:**
     ```
     npx prisma migrate deploy && npm start
     ```
   - **Instance Type:** Free

Остальные шаги такие же как в Варианте А.

## Шаг 5: Деплой Frontend (1 минута)

1. Нажмите **"New +"** → **"Static Site"**
2. Выберите тот же репозиторий **RC2**
3. Заполните:
   - **Name:** `rentcontrol-frontend`
   - **Region:** Frankfurt
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым
   - **Build Command:**
     ```
     npm install && npm run build
     ```
   - **Publish Directory:** `dist`

4. Добавьте Environment Variable:

   | Key | Value |
   |-----|-------|
   | `VITE_API_URL` | *вставьте URL backend из шага 4* |

5. Нажмите **"Create Static Site"**
6. ⏳ Подождите 2-3 минуты

## Шаг 6: Финальная настройка (30 секунд)

1. Вернитесь к настройкам **rentcontrol-backend**
2. В Environment Variables найдите `CORS_ORIGIN`
3. Измените `*` на URL вашего frontend (например: `https://rentcontrol-frontend.onrender.com`)
4. Нажмите **"Save Changes"**
5. Backend автоматически перезапустится (1 минута)

## Шаг 7: Инициализация данных (опционально, 1 минута)

Если хотите добавить тестовых пользователей:

1. Перейдите к **rentcontrol-backend**
2. Нажмите **"Shell"** (в правом верхнем углу)
3. Выполните:
   ```bash
   cd server
   npx prisma db seed
   ```
4. Дождитесь "Seeding completed successfully!"

## ✅ Готово!

Ваше приложение доступно по адресу:
- **Frontend:** `https://rentcontrol-frontend.onrender.com`
- **Backend API:** `https://rentcontrol-backend.onrender.com/api`

### Тестовые учетные данные:
- **Admin:** `admin` / `admin123`
- **Manager:** `manager` / `manager123`
- **Supervisor:** `supervisor` / `supervisor123`

---

## 🔧 Если что-то пошло не так

### Backend не запускается
1. Проверьте логи на странице backend
2. Убедитесь, что `DATABASE_URL` правильный (копировали Internal URL)
3. Проверьте, что все Environment Variables установлены

### Frontend показывает ошибки API
1. Откройте консоль браузера (F12)
2. Проверьте, что `VITE_API_URL` указывает на правильный URL backend
3. Проверьте, что backend доступен: откройте `https://your-backend.onrender.com/api/health`

### База данных пустая
Выполните seed (см. Шаг 7)

---

## 📊 Что дальше?

После успешного деплоя рекомендуется:

1. **Добавить мониторинг:**
   - Зарегистрируйтесь на [UptimeRobot](https://uptimerobot.com) (бесплатно)
   - Добавьте мониторинг для `https://your-backend.onrender.com/api/health`

2. **Настроить кастомный домен** (опционально):
   - В настройках Static Site нажмите "Custom Domains"
   - Следуйте инструкциям для добавления вашего домена

3. **Настроить резервное копирование:**
   - В настройках PostgreSQL включите автоматические снапшоты

---

## 💡 Важные замечания

- ⚠️ **Free tier засыпает после 15 минут неактивности**
  - При первом запросе потребуется 30-60 секунд для "пробуждения"
  - Для production использования рекомендуется платный план ($7/месяц)

- 🔒 **Безопасность:**
  - Измените `JWT_SECRET` на более сложный
  - Обновите `CORS_ORIGIN` на точный URL frontend
  - Измените пароли тестовых пользователей

- 💾 **База данных:**
  - Free tier: 256MB хранилища
  - Автоматическое удаление через 90 дней неактивности
  - Для production рекомендуется платный план

---

## 🎉 Поздравляем!

Ваше приложение опубликовано и доступно онлайн! 🚀

**Поделитесь ссылкой:** `https://rentcontrol-frontend.onrender.com`

---

**Нужна помощь?** 
- Полная документация: [DEPLOYMENT.md](DEPLOYMENT.md)
- Чеклист: [DEPLOYMENT_CHECKLIST.md](DEPLOYMENT_CHECKLIST.md)
- GitHub Issues: https://github.com/Alphpage/RC2/issues
