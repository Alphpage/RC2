# 🌱 Seed Database with Test Data

## Проблема: Не могу войти (admin/manager не работают)

**Причина:** База данных пустая - нет тестовых пользователей.

**Решение:** Запустить seed скрипт для создания тестовых данных.

---

## 🚀 Как запустить seed на Render (2 минуты):

### Вариант 1: Через Shell (рекомендуется)

1. **Откройте Render Dashboard:**
   https://dashboard.render.com

2. **Найдите Backend Service:**
   - Web Services → `rentcontrol-backend`

3. **Откройте Shell:**
   - Нажмите кнопку **"Shell"** (справа вверху)
   - Откроется терминал в браузере

4. **Выполните команды:**
   ```bash
   cd server
   npx prisma db seed
   ```

5. **Дождитесь завершения:**
   Вы увидите:
   ```
   🌱 Seeding database...
   ✅ Created user: admin (ADMIN)
   ✅ Created user: manager (MANAGER)
   ✅ Created user: supervisor (SUPERVISOR)
   ✅ Created rental point: Точка Центр
   ✅ Created rental point: Парк Победы
   ✅ Created rental point: Набережная
   ✅ Created cash register: Касса 1 (Центр)
   ...
   🎉 Seeding completed successfully!
   
   📝 Default users created:
      Admin:      login: admin      | password: admin123
      Manager:    login: manager    | password: manager123
      Supervisor: login: supervisor | password: supervisor123
   ```

6. **Готово!** Теперь можете войти с этими пользователями.

---

### Вариант 2: Локально (если есть доступ к БД)

Если вы хотите запустить seed локально и подключиться к Render PostgreSQL:

1. **Получите Database URL:**
   - Render Dashboard → PostgreSQL → Connection Info
   - Скопируйте **Internal Database URL**

2. **Создайте `.env` в `server/` директории:**
   ```bash
   cd server
   echo "DATABASE_URL=postgresql://..." > .env
   ```

3. **Запустите seed:**
   ```bash
   npm run prisma:generate
   npx prisma db seed
   ```

---

## 📊 Что создает seed скрипт:

### Пользователи (3 шт):
| Логин | Пароль | Роль | Описание |
|-------|--------|------|----------|
| `admin` | `admin123` | ADMIN | Полный доступ |
| `manager` | `manager123` | MANAGER | Только чтение |
| `supervisor` | `supervisor123` | SUPERVISOR | Управление точкой "Центр" |

### Точки проката (3 шт):
- Точка Центр (10% к зарплате)
- Парк Победы (5% к зарплате)
- Набережная (7% к зарплате)

### Кассы (4 шт):
- Касса 1 (Центр)
- Терминал 1 (Центр)
- Основная (Парк)
- Касса Набережная

### Сотрудники (4 шт):
- Иванов Иван - Старший оператор (300 р/час)
- Петрова Анна - Оператор (250 р/час)
- Сидоров Олег - Оператор (280 р/час)
- Смирнова Елена - Стажер (200 р/час)

### Вопросы аудита (4 шт):
- 2 утренних вопроса (полы, техника)
- 2 вечерних вопроса (мусор, зарядка)

---

## 🧪 Проверка после seed:

### 1. Проверьте вход:
```
URL: https://rentcontrol-frontend.onrender.com
Логин: admin
Пароль: admin123
```

### 2. Проверьте данные в БД (через Render):
- Dashboard → PostgreSQL → Connect
- Или используйте **Prisma Studio**:
  ```bash
  cd server
  npx prisma studio
  ```

### 3. Проверьте через API:
```bash
# Login
curl -X POST https://your-backend.onrender.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"login":"admin","password":"admin123"}'

# Должен вернуть токен и данные пользователя
```

---

## ⚠️ Важные моменты:

### 1. Seed можно запускать многократно
Скрипт использует `upsert` для пользователей, поэтому:
- ✅ Если пользователь существует - не создает дубликат
- ✅ Можно запускать несколько раз безопасно
- ⚠️ Но точки/кассы/сотрудники будут дублироваться!

### 2. Если хотите очистить БД:
```bash
cd server
npx prisma migrate reset
npx prisma db seed
```
**⚠️ Осторожно!** Это удалит ВСЕ данные!

### 3. Seed автоматически при деплое (опционально):
Можно добавить в **Start Command** на Render:
```bash
npx prisma migrate deploy && npx prisma db seed; npm start
```
Но это будет дублировать данные при каждом деплое!

---

## 🔧 Альтернатива: Добавить пользователей вручную через API

Если Shell не работает, можно создать пользователей через API:

### Создайте файл `create-users.js`:
```javascript
const bcrypt = require('bcryptjs');

async function createUser(login, password, name, role) {
  const hashedPassword = await bcrypt.hash(password, 10);
  
  const response = await fetch('https://your-backend.onrender.com/api/users', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': 'Bearer YOUR_ADMIN_TOKEN' // После создания первого admin
    },
    body: JSON.stringify({
      login,
      password: hashedPassword,
      name,
      role,
      assignedPointIds: []
    })
  });
  
  return response.json();
}

// Но сначала нужен хотя бы один admin...
```

**Проблема:** Нужен первый admin для создания других пользователей! 🐔🥚

**Поэтому используйте Shell - это проще!**

---

## 📝 Пошаговая инструкция (для новичков):

### Шаг 1: Откройте Render
1. Зайдите на https://dashboard.render.com
2. Войдите в свой аккаунт

### Шаг 2: Найдите Backend
1. В списке сервисов найдите `rentcontrol-backend`
2. Кликните на него

### Шаг 3: Откройте Shell
1. Справа вверху есть кнопка **"Shell"**
2. Кликните на неё
3. Откроется терминал в браузере

### Шаг 4: Запустите seed
1. Введите: `cd server`
2. Нажмите Enter
3. Введите: `npx prisma db seed`
4. Нажмите Enter
5. Дождитесь завершения (10-20 секунд)

### Шаг 5: Проверьте результат
Вы увидите список созданных данных:
```
✅ Created user: admin (ADMIN)
✅ Created user: manager (MANAGER)
...
🎉 Seeding completed successfully!
```

### Шаг 6: Войдите в систему
1. Откройте https://rentcontrol-frontend.onrender.com
2. Логин: `admin`
3. Пароль: `admin123`
4. Нажмите "Войти"

### ✅ Готово!

---

## 🆘 Если Shell не работает:

### Альтернатива: Через локальный терминал

1. **Установите Render CLI:**
   ```bash
   npm install -g @render.com/cli
   ```

2. **Login в Render:**
   ```bash
   render login
   ```

3. **Подключитесь к Shell:**
   ```bash
   render shell <your-service-id>
   ```

4. **Запустите seed:**
   ```bash
   cd server
   npx prisma db seed
   ```

---

## 📚 Дополнительные ресурсы:

- **Prisma Seed Documentation:** https://www.prisma.io/docs/guides/database/seed-database
- **Render Shell Documentation:** https://render.com/docs/shell
- **Seed скрипт:** `server/prisma/seed.ts`

---

## ✅ Чеклист:

- [ ] Открыли Render Dashboard
- [ ] Нашли Backend Service
- [ ] Открыли Shell
- [ ] Выполнили `cd server`
- [ ] Выполнили `npx prisma db seed`
- [ ] Увидели "🎉 Seeding completed successfully!"
- [ ] Зашли в приложение как `admin` / `admin123`
- [ ] ✅ Всё работает!

---

**Сообщите, когда выполните - проверим, что всё заработало! 🚀**
